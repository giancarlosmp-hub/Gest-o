DATABASE_URL=postgresql://postgres@db:5432/salesforce_pro?schema=public
PORT=4000
FRONTEND_URL=https://crm.demetraagronegocios.com.br
CORS_ALLOWED_ORIGINS=https://crm.demetraagronegocios.com.br
JWT_ACCESS_SECRET=super-access
JWT_ACCESS_EXPIRES_IN=12h
JWT_REFRESH_SECRET=super-refresh
VITE_API_URL=https://crm.demetraagronegocios.com.br/api
ENABLE_SMOKE_BOOTSTRAP=false
SEED_ON_BOOTSTRAP=false
ADMIN_BOOTSTRAP_ENABLED=false

# Integração backend para consulta segura de CNPJ
# O docker-compose repassa estas variáveis diretamente para o serviço `api`.
# Produção/VPS: configure no arquivo .env do backend/compose e faça redeploy sem apagar volumes.
# Defaults seguros para BrasilAPI (sem API key obrigatória):
CNPJ_LOOKUP_PROVIDER=brasilapi
CNPJ_LOOKUP_BASE_URL=https://brasilapi.com.br/api/cnpj/v1
# Opcional para providers que exigem autenticação; nunca exponha no frontend
CNPJ_LOOKUP_API_KEY=
# Exemplo com provider genérico:
# CNPJ_LOOKUP_PROVIDER=generic
# CNPJ_LOOKUP_BASE_URL=https://api.seu-provedor.com/cnpj/{cnpj}
# CNPJ_LOOKUP_API_KEY=seu-token-aqui

# Integração ERP UltraFV3 (ponte única com o ERP local; não conectar direto no Firebird)
# Chave usada para criptografar credenciais ERP por vendedor (obrigatória em produção)
ERP_CREDENTIAL_ENCRYPTION_KEY=<generate-a-long-secret-key>
# Configure a URL da API UltraFV3 exposta pelo servidor Windows local, normalmente na porta 8585.
ULTRAFV3_BASE_URL=http://servidor-ultrafv3.local:8585
ULTRAFV3_USERNAME=
ULTRAFV3_PASSWORD=

# Camada única de Inteligência Artificial (backend)
# AI_PROVIDER define o provider ativo. O primeiro provider oficial é OpenRouter (openrouter).
# AI_CHAT_ENABLED=false mantém os fluxos no comportamento determinístico atual.
# AI_BASE_URL deve apontar para uma API compatível com OpenAI Chat Completions.
# AI_API_KEY é obrigatória quando AI_CHAT_ENABLED=true; nunca exponha no frontend.
# AI_MODEL define o modelo remoto usado pelo provider configurado.
# AI_TIMEOUT_MS limita cada chamada de IA antes do fallback determinístico.
# AI_MAX_OUTPUT_TOKENS limita a saída gerada pelo modelo.
# AI_TEMPERATURE controla a criatividade da resposta.
AI_PROVIDER=openrouter
AI_CHAT_ENABLED=true
AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=
AI_MODEL=deepseek/deepseek-chat-v3
AI_TIMEOUT_MS=30000
AI_MAX_OUTPUT_TOKENS=512
AI_TEMPERATURE=0.4
# Preparado para provider Ollama futuro; não inicia Ollama automaticamente.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b
