# Rendered by Preview Deploy. owner-pr=__PREVIEW_OWNER_PR__ owner-run=__PREVIEW_OWNER_RUN_ID__ owner-attempt=__PREVIEW_OWNER_RUN_ATTEMPT__
server {
    listen 80;
    listen [::]:80;
    server_name __PREVIEW_HOST_TLS__ __PREVIEW_HOST_HTTP__;
    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://127.0.0.1:__PREVIEW_WEB_PORT__;
    }
}
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name __PREVIEW_HOST_TLS__ __PREVIEW_HOST_HTTP__;
    ssl_certificate /etc/letsencrypt/live/crm.demetraagronegocios.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/crm.demetraagronegocios.com.br/privkey.pem;
    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://127.0.0.1:__PREVIEW_WEB_PORT__;
    }
}
